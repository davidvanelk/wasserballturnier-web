apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: matomo
spec:
  secretName: matomo-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  dnsNames:
    - matomo.wasserball.elk-software.de
